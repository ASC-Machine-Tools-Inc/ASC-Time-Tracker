// Display a modal prompting the user for their employee ID and name if we
// don't already have them in localstorage. Heinously insecure, but since
// this is all internal it should be fine for now.

function verifyUser() {
}